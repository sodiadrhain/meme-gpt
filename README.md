# MEME GPT
A simple web3 tool fioir trading activities.

### Setup

### Prerequisites

- Python 3.8 or higher
- PIP

### Running App locally
Clone the [repository](https://github.com/sodiadrhain/meme-gpt.git) and proceed with the instructions below.

Install all requirements and dependancies

```
pip install -r requirements.txt
```

Then run

```
python main.py
```

### Running in a venv

```
python -m venv venv
```
```
source venv/bin/activate
```
```
pip install -r requirements.txt
```

Then run

```
python main.py
```

Note: if you have `python3` and `pip3` command installed use `python3` instead of `python` and `pip`

